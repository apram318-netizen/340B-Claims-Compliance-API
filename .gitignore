# Environment & secrets — NEVER commit these
.env
.env.local
.env.*.local
*.pem
*.key
*.p12
*.pfx

# Compiled binaries
bin/
dist/
api/api
worker/worker

# Test output & coverage
*.test
*.out
coverage.html
coverage.txt

# Generated export files
exports/

# OS artifacts
.DS_Store
Thumbs.db

# Editor & IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Go build cache
vendor/

# Claude Code configuration — not part of the project
.claude/
memory/

# Kubernetes secrets (populated templates only — commit the template, not the secret)
k8s/secret.yaml
